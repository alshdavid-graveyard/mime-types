# Repo setup

Install dependencies via your package manager of choice

```bash
npm install
pnpm install
yarn install
```

Use npm scripts to build/test the project

```
npm run build
npm run test
```

That's all folks!
